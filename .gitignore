# Python
__pycache__/
*.pyc

# ngspice / SPICE artifacts (keep SIM_RESULTS.txt — it's checked-in evidence)
*.raw
*.out

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# KiCad — backups, locks, autosave, per-user state
*-backups/
*.kicad_prl
*.bak
*.lck
~*.lck
*auto_saved_files*
_autosave-*
fp-info-cache/

# vendor symbol/footprint import scaffolding (we use stock libs now)
therm_kicad/therm/library_objects/
therm_kicad_fp.pretty/
